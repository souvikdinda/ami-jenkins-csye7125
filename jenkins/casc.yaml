credentials:
  system:
    domainCredentials:
    - credentials:
      - secretText:
          scope: GLOBAL
          secret: "${GITHUB_CREDENTIALS}"
          description: "GitHub access token"
          id: "github-token"