credentials:
  system:
    domainCredentials:
      - credentials:
          - string:
              scope: SYSTEM
              id: "github-token"
              secret: "${GITHUB_TOKEN}"
              description: "GitHub Token"

      # - secretText:
      #    scope: GLOBAL
      #    secret: "${GITHUB_CREDENTIALS}"
      #    description: "GitHub access token for webapp-db repository"
      #    id: "github-webapp-db-token" 